{
  "name": "sentinelweb-render-entry",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "build": "npm --prefix server install --production",
    "start": "npm --prefix server run start",
    "dev": "npm --prefix server run dev",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
