{
  "name": "sentinelweb-render-entry",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "postinstall": "npm ci --prefix server --omit=dev",
    "build": "npm ci --prefix server --omit=dev",
    "start": "npm --prefix server run start",
    "dev": "npm --prefix server run dev"
  }
}