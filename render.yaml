services:
  - type: web
    name: sentinelweb
    env: node
    rootDir: server
    buildCommand: npm ci
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Optimize for faster cold starts
    plan: starter